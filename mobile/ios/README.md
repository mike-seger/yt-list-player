# iOS Placeholder

This directory is reserved for the iOS app shell.

Planned next phase:

- Create a WKWebView wrapper app.
- Mirror command bridge used by Android (`window.MobileHost.command`).
- Add `AVAudioSession`, `MPRemoteCommandCenter`, and `NowPlayingInfo` integration.
- Define asset-sync workflow from `web/` to iOS bundle.
